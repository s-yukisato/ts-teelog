{
  "name": "teelog",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./server/index.ts",
    "heroku-postbuild": "cd client && yarn install && yarn build && cd ../server && yarn install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/s-yukisato/ts-book-management.git"
  },
  "engines": {
    "node": "^14.0.x",
    "yarn": "1.x"
  }
}
